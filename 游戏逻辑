开始游戏：
main.js 71行 sence.switchSence( "game-body" );
scene.js 141行 game.start();
game.js 
44行 start：
	在start中执行类似于重载自js的setTimeout，fn是要执行的函数，执行fn时，通过setInterval设置了一个重复的周期，重复执行barbette
	volleyNum控制水果数量
30行 barbette:
	创建fruit，调用shotout抛出

在raphael.js中声明了一些类似于mousemove的响应，然后在ucren里加入了对应的响应函数
game.start只在游戏开始时执行

椭圆 e=c/a, c是焦点到圆心的距离，a是半长轴
fruit中的半径就是半长轴。

rotateSpeed数组记载了旋转速度
shotout中rotate函数第二个参数控制旋转速度

all.js 972行控制onshotouting的执行间隔


todolist：
1. 动态生成直线逻辑 done
all.js1850中layer.js可以创建一个div，设置线段坐标就可以创建直线了。现在是如何删除它。
写一个新的knife类
2. 生成多个水果 done
3. 加两个button，一个负责生成平行水果，一个负责生成不平行水果
（1）fruit.js 194行 控制水果是否旋转， 114行 得到旋转速度，280行第一个参数就是旋转角度。旋转时的两个端点可以通过连线来显示一下。
（2）平行的话，就是不旋转，处理一下每张水果图片的垂直图片就可以了。

控制一下onShotOuting的更新频率，会不会能显示直线？
answer line 在set函数值中的life增加一点会不会好点？


3. 加一个button，一按就能显示目标直线
4. 加一个对偶的显示界面

